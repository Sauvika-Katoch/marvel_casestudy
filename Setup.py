from setuptools import setup, find_packages

setup(
    name='CharExtractor',
    version='0.0.1',
    description='This is a very simple API call to extract the charcter list from Marvel website and filter them.',
    long_description=open('README.txt').read()+'\n\n'+open('CHANGELOG.txt').read(),
    url='',
    author='Sauvika Katoch',
    keywords='',
    packages=find_packages())